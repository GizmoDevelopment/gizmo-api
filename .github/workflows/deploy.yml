  name: deploy

  on:
    release:
<<<<<<< HEAD
      - types: [published]
=======
      - published
>>>>>>> fde7f6b237498862cabd156be288b00302507dd2

  jobs:
    cd:
      runs-on: ${{ matrix.os }}

      strategy:
        matrix:
          os: [ubuntu-latest]
          node: [14]

      steps:
        - name: Checkout
          uses: actions/checkout@master

        - name: Setup node env
          uses: actions/setup-node@v2.1.2
          with:
            node-version: ${{ matrix.node }}

        - name: Install dependencies
          run: npm i

        - name: Build module
          run: npm run build:node

        - name: Remove useless files and directories
          run: rm -rf ./src && rm -rf ./node_modules && rm -rf ./build && rm -rf ./dist

        - name: Deploy
          uses: JS-DevTools/npm-publish@v1
          with:
            token: ${{ secrets.NPM_TOKEN }}
            check-version: true