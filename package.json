{
  "name": "gizmo-api",
  "version": "1.2.2",
  "description": "A minimal wrapper for interfacing with the Gizmo API.",
  "main": "./lib/index.js",
  "typings": "./lib/index.d.ts",
  "keywords": [
    "gizmo",
    "gizmo-api",
    "gizmo api"
  ],
  "scripts": {
    "prepare": "ts-patch install -s",
    "lint": "eslint --ext .ts,.js ./src",
    "test": "mocha ./tests/api.js",
    "build:ts-node": "tsc -p tsconfig.node.json",
    "build:ts-browser": "tsc -p tsconfig.browser.json",
    "build:js-bundle": "esbuild ./dist/index.js --bundle --minify --global-name=gizmo --outfile=./bundle/gizmo-api.bundle.js",
    "build:rename-bundle": "node ./dev/rename-bundle.js",
    "build:fix-bundle-version-in-readme": "node ./dev/fix-bundle-version-in-readme.js",
    "build:node": "rm -rf ./lib && npm run build:ts-node",
    "build:browser": "rm -rf ./dist && npm run build:ts-browser && npm run build:js-bundle && npm run build:rename-bundle && npm run build:fix-bundle-version-in-readme",
    "build": "npm run lint && npm run build:node && npm run build:browser && npm run test"
  },
  "files": [
    "lib"
  ],
  "engines": {
    "node": ">=12.x"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/GizmoDevelopment/gizmo-api"
  },
  "author": "Tjaz (GizmoTjaz)",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/GizmoDevelopment/gizmo-api/issues"
  },
  "homepage": "https://github.com/GizmoDevelopment/gizmo-api/#readme",
  "devDependencies": {
    "@gizmo-dev/eslint-plugin": "^3.2.0",
    "@types/node": "^12.20.55",
    "esbuild": "^0.14.49",
    "eslint": "^8.19.0",
    "mocha": "^9.2.2",
    "ts-patch": "^2.0.1",
    "typescript": "^4.7.4",
    "typescript-transform-paths": "^3.3.1"
  },
  "dependencies": {
    "cross-fetch": "^3.1.5"
  },
  "browser": {
    "cross-fetch": false
  }
}
